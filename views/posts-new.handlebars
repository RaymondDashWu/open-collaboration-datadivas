<div class="row">
    <div class="col-sm-6 col-sm-offset-3">
          <form method="POST" action="/projects/{{projectId}}/posts">
              {{> posts-form}}
              <input type="hidden" name="projectId" value="{{projectId}}">
              <div class="form-group">
                <button class="btn btn-primary" type="submit">Save Project Post</button>
              </div>
          </form>
      </div>
</div>
